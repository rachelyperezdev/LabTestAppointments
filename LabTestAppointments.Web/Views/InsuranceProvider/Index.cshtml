@using LabTestAppointments.Data.Entities
@model IEnumerable<LabTestAppointments.Data.Entities.InsuranceProvider>
@{
    ViewData["Title"] = "Proveedores de seguros médicos";
}

<div class="container-fluid">
    <div class="row">
        <div class="row">
            <div class="col-9">
                <h1 class="display-4">Proveedores de seguros médicos</h1>
            </div>
            <div class="col-3">
                <p><a asp-controller="InsuranceProvider" asp-action="Create">Agregar</a></p>
            </div>
        </div>
        <div class="col-12 mt-2">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Teléfono</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int counter = 1;
                    }
                    @foreach (LabTestAppointments.Data.Entities.InsuranceProvider insuranceProvider in Model)
                    {
                        <tr>
                            <th scope="row">@counter</th>
                            <td>@insuranceProvider.Name</td>
                            <td>@insuranceProvider.Phone</td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = insuranceProvider.Id })
                                @Html.ActionLink("Detalles", "Details", new { id = insuranceProvider.Id })
                            </td>
                        </tr>
                        counter++;
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>