@using LabTestAppointments.Data.Entities﻿
@model IEnumerable<LabTestAppointments.Data.Entities.Bioanalyst>

@{
    ViewData["Title"] = "Bioanalistas";
}

<div class="container-fluid">
    <div class="row">
        <div class="row">
            <div class="col-9">
                <h1 class="display-4">Bioanalista</h1>
            </div>
            <div class="col-3">
                <p><a asp-controller="Bioanalyst" asp-action="Create">Agregar</a></p>
            </div>
        </div>
        <div class="col-12 mt-2">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Apellido</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int counter = 1;
                    }
                    @foreach (LabTestAppointments.Data.Entities.Bioanalyst bioanalyst in Model)
                    {
                        <tr>
                            <th scope="row">@counter</th>
                            <td>@bioanalyst.FirstName</td>
                            <td>@bioanalyst.LastName</td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = bioanalyst.Id })
                                @Html.ActionLink("Detalles", "Details", new { id = bioanalyst.Id })
                            </td>
                        </tr>
                        counter++;
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>